import React from "react";

const UserInfo = () => {
  return (
    <div>
      {props.user.map((info) => (
        <div key={info.id}>
          <h2>info.name</h2>
          <p>info.email</p>
        </div>
      ))}
    </div>
  );
};

export default UserInfo;
